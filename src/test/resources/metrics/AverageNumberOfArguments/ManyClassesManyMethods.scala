class One { // 0
  def one(): Unit = {
  }
}

class Two { // 1.5
  def one(first: Int): Unit = {
  }
  def two(first: Int, second: Int): Unit = {
  }
}

class Three { // 2
  def one(first: Int): Unit = {
  }
  def two(first: Int, second: Int): Unit = {
  }
  def three(first: Int, second: Int, third: Int): Unit = {
  }
}